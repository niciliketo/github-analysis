<p>
  <strong>User:</strong>
  <%= @repo.user.name %>
</p>

<p>
  <strong>Pull requests:</strong>
  <%= @repo.pull_requests.count %>
</p>

<p>
  <strong>Full Name:</strong>
  <%= @repo.full_name %>
</p>

<p>
  <strong>Last checked:</strong>
  <%= @repo.checked_at %>
</p>
<%= link_to 'Pull Requests', repo_pull_requests_path(@repo) %> |
<%= link_to 'Edit', edit_repo_path(@repo) %> |
<%= link_to 'Back', repos_path %>
