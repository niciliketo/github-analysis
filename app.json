{
  "name": "github-analysis",
  "scripts": {
    "dokku": {
      "predeploy": "yarn; bundle exec rake assets:precompile",
      "postdeploy": "bundle exec rake db:migrate"
    }
  },
  "cron": [
    {
      "command": "bundle exec rails runner lib/scripts/generate_data.rb -e production",
      "schedule": "@hourly"
    }
  ]
}
